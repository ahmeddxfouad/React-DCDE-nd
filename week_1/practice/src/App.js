import logo from './logo.svg';
import './App.css';
import TodoList from "./components/TodoList";
//this tutorial is all about how to create elements and components in react.
/*
  1. create a TodoList component -> Done
  2. Render the TodoList component -> Done
  3. Explore composition with the TodoList and TodoItem components -> Done
  4. Explore the unidirectional data flow with the TodoList component -> Done

*/
function App() {
  return (
      <TodoList/>
  );
}

export default App;
